<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title><%= song.title %> - <%= song.artist %></title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f0f2f5;
      margin: 0;
      padding: 40px;
      direction: rtl;
      text-align: right;
      color: #333;
    }

    .container {
      background-color: #ffffff;
      padding: 30px;
      max-width: 600px;
      margin: auto;
      border-radius: 12px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.07);
      text-align: center;
    }

    h1 {
      font-size: 26px;
      margin-bottom: 10px;
      color: #2c3e50;
    }

    h3 {
      font-size: 18px;
      color: #7f8c8d;
      margin-bottom: 25px;
    }

    audio {
      width: 100%;
      outline: none;
    }

    .note {
      font-size: 13px;
      color: #aaa;
      margin-top: 20px;
    }

    .buttons {
      margin-top: 25px;
      display: flex;
      flex-direction: column;
      gap: 10px;
      align-items: center;
    }

    .btn {
      background-color: #3498db;
      color: #fff;
      padding: 10px 18px;
      border: none;
      border-radius: 6px;
      font-size: 15px;
      cursor: pointer;
      text-decoration: none;
      transition: background-color 0.3s;
    }

    .btn:hover {
      background-color: #2980b9;
    }

    .success {
      color: green;
      font-size: 14px;
      margin-top: 10px;
    }

    /* 🔒 إخفاء زر التحميل */
    audio::-internal-media-controls-download-button {
      display: none;
    }

    audio::-webkit-media-controls-enclosure {
      overflow: hidden;
    }

    audio::-webkit-media-controls-panel {
      width: 100% !important;
    }

    /* 🔒 منع تحديد النص والنسخ */
    * {
      user-select: none;
    }
  </style>
</head>
<body oncontextmenu="return false" onkeydown="blockKeys(event)">
  <div class="container">
    <h1><%= song.title %></h1>
    <h3>بواسطة: <%= song.artist %></h3>

    <% if (song.visibility === 'private') { %>
      <audio controls controlsList="nodownload" oncontextmenu="return false" draggable="false">
        <source src="/stream/<%= song.url_code %>" type="audio/mpeg">
        متصفحك لا يدعم تشغيل الصوت.
      </audio>
      <div class="note" style="margin-top: 25px;">🔒 هذه الأغنية خاصة ولا يمكن تحميلها</div>
    <% } else { %>
      <audio controls>
        <source src="<%= song.url %>" type="audio/mpeg">
        متصفحك لا يدعم تشغيل الصوت.
      </audio>

      <div class="buttons">
        <a class="btn" href="<%= song.url %>" download>تحميل الأغنية</a>
        <button class="btn" onclick="copyLink()">مشاركة الرابط</button>
        <div id="copySuccess" class="success" style="display:none;">تم نسخ الرابط ✅</div>
      </div>
    <% } %>

    <div class="note">تم تحميل الصوت من Firebase</div>
  </div>

  <script>
    function copyLink() {
      const text = window.location.href;
      navigator.clipboard.writeText(text)
        .then(() => {
          document.getElementById('copySuccess').style.display = 'block';
          setTimeout(() => {
            document.getElementById('copySuccess').style.display = 'none';
          }, 3000);
        })
        .catch(() => alert('❌ فشل نسخ الرابط'));
    }

    // 🔒 تعطيل أزرار الحفظ والتنزيل من الكيبورد
    function blockKeys(e) {
      const forbiddenKeys = ['s', 'S', 'u', 'U'];
      if ((e.ctrlKey || e.metaKey) && forbiddenKeys.includes(e.key)) {
        e.preventDefault();
        alert("🚫 لا يمكنك استخدام هذا الاختصار");
      }
    }
  </script>
</body>
</html>
